코딩 메모장

문자열 -> 배열
[char]
char[] chars = my_string.toCharArray();      문자열을 문자 배열로 변환
char temp = chars[num1];                     (교환을 위해 임시로 저장)
chars[num1] = chars[num2];                   (num2의 문자를 num1 자리에)
chars[num2] = temp;                          (임시 저장했던 문자를 num2 자리에)
return new String(chars);                     다시 배열을 문자열로 바꿔서 반환
[sb]

char ch1 = sb.charAt(num1);	                  num1 위치 문자 추출
char ch2 = sb.charAt(num2);                   num2 위치 문자 추출
sb.setCharAt(num1, ch2);	                  num1 위치에 ch2 설정
sb.setCharAt(num2, ch1);                   	  num2 위치에 ch1 설정
sb.toString();	                              다시 문자열로 반환


문자열 대문자 소문자
Character.isUpperCase(ch)	                  ch가 대문자인지 확인
Character.isLowerCase(ch)	                  ch가 소문자인지 확인
Character.toUpperCase(ch)	                  ch를 대문자로 변환
Character.toLowerCase(ch)	                  ch를 소문자로 변환

StringBuilder                                 주요 메서드:
append()	                                  문자열 끝에 붙이기
insert()	                                  원하는 위치에 삽입
delete()	                                  특정 부분 삭제
reverse()	                                  문자열 뒤집기
toString()	                                  최종 문자열로 변환


배열
import java.util.*;                                            코드쓰기위한 임포트
List<Integer> list = new ArrayList<>();	                       동적으로 크기 지정 없이 약수 저장 가능
list.add(i);	                                               약수를 발견할 때마다 추가
return list.stream().mapToInt(Integer::intValue).toArray();    리스트를 배열로 한 줄 변환


붙이기(배열)
StringBuilder sb = new StringBuilder(); //선언
for (String s : arr) {
    sb.append(s);
}
System.out.println(sb.toString());

비교(배열)
==의 상위 s1[ i ].equals(s2[ j ])

더미 배열
import java.util.*;                     //사용하기 위한 조건
List<Integer> list = new ArrayList<>(); //생성 <성질>
list.add(숫자);                          //값을 추가
list.size();                            //.length와 같은 코드
list.get(인덱스)                         //'인덱스'번째 요소 가져옴
list.set(인덱스, 새값)                    // 인덱스 -> 새값
list.stream().mapToInt(Integer::intValue).toArray(); // Integer->Int 변환(삽입시 필요)
list.toArray(new String[0]); // list<String> -> String[ ]

배열
import java.util.Arrays;                       // 사용하기에 필요한것
answer = new int[numbers.length];              // numbers의 길이만큼 배열 갯수 지정
Arrays.sort(arr);                              // 오름차순으로 정렬
Arrays.sort(arr, Collections.reverseOrder());  // 내림차순
char[] chars = sb.toString().toCharArray();    // sb를 문자열로 변환 -> 문자배열(char[])
int[] answer = new int[chars.length];          //chars.length 만큼 크기의 int배열 제작
Arrays.copyOfRange(원본배열, 시작인덱스, 끝인덱스); // 배열자르기

char코드
String str = "a1b2c3";

for (int i = 0; i < str.length(); i++) {
    char c = str.charAt(i);  //
    if (c >= '0' && c <= '9') {
        System.out.println("숫자 문자: " + c);
    }

향상된 for문
for (int num : array)
array배열에서 하나씩 꺼내 num에 담는다
---------------------------------------
for (int num : array) {
            if (num == n) answer++;
}
---------------------------------------

if문
if (answer > n) break;  //break의 경우 가능
    answer += numbers[i];  // else부분

for-each문
for (배열값 :배열이름) { //배열에 있는 숫자를 반복으로 가져옴
    if (answer > n) break; //조건
    answer += num; // 조건
}

삼항 연산자
return (str1.contains(str2)? 1: 2);
             조건  참 거짓

 double discount = price >= 500_000 ? 0.2
         : price >= 300_000 ? 0.1
         : price >= 100_000 ? 0.05
         : 0;                                            // discount는 price의 조건에 의해 정해